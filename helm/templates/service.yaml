apiVersion: v1
kind: Service
metadata:
  name: {{ include "service-account.fullname" . }}
  labels:
    {{- include "service-account.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "service-account.selectorLabels" . | nindent 4 }}
